<div class="d-flex justify-content-between align-items-center mb-4">
    <ul class="nav nav-tabs border-bottom-0">
        <li class="nav-item">
            <a class="nav-link active fw-bold text-primary border-0 border-bottom border-primary border-3 px-0 pb-2 fs-5" style="border-radius: 0;" href="#">User</a>
        </li>
    </ul>
    <button class="btn btn-primary px-3 py-2 rounded-3 fw-bold d-flex align-items-center gap-2" style="background-color: #3b82f6; border: none;">
        <span class="fs-5 lh-1">+</span> Tambah User
    </button>
</div>

<div class="card border-0 shadow-sm rounded-4 mb-4 bg-white text-dark">
    <div class="card-header bg-white border-0 pt-4 px-4 d-flex justify-content-between align-items-center">
        <h5 class="fw-bold text-dark mb-0">List Data</h5>
        <span class="text-secondary fs-5"><i class="bi bi-chevron-up"></i></span>
    </div>

    <div class="card-body px-4 pb-4 bg-white text-dark">
        <form method="GET" action="" class="row g-3 mb-3 align-items-center">
            <div class="col-auto">
                <select name="limit" class="form-select text-dark fw-semibold bg-white border-secondary-subtle rounded-3" style="width: 75px;">
                    <option value="10">10</option>
                    <option value="25">25</option>
                    <option value="50">50</option>
                </select>
            </div>
            <div class="col-md-3">
                <div class="input-group">
                    <span class="input-group-text bg-white border-end-0 border-secondary-subtle text-secondary"><i class="bi bi-search"></i></span>
                    <input type="text" name="search" class="form-control border-start-0 ps-0 text-dark bg-white border-secondary-subtle" placeholder="Cari" value="<?= isset($_GET['search']) ? $this->escape($_GET['search']) : ''; ?>">
                </div>
            </div>
            <div class="col-md-2">
                <select name="level" class="form-select text-secondary bg-white border-secondary-subtle rounded-3">
                    <option value="">Pilih Level User</option>
                    <option value="IT Provider">IT Provider</option>
                    <option value="MIS">MIS</option>
                </select>
            </div>
            <div class="col-md-2">
                <select name="role" class="form-select text-secondary bg-white border-secondary-subtle rounded-3">
                    <option value="">Pilih Role</option>
                    <option value="Administrator">Administrator</option>
                    <option value="Viewer">Viewer</option>
                </select>
            </div>
            <div class="col-md-2">
                <select name="status" class="form-select text-secondary bg-white border-secondary-subtle rounded-3">
                    <option value="">Pilih Status</option>
                    <option value="Aktif">Aktif</option>
                    <option value="Non-Aktif">Non-Aktif</option>
                    <option value="Blokir">Blokir</option>
                </select>
            </div>
            <div class="col-auto">
                <button type="submit" class="btn btn-outline-primary px-4 fw-bold rounded-3" style="min-width: 90px;">Search</button>
            </div>
        </form>

        <div class="d-flex align-items-center gap-3 mb-4">
            <span class="badge px-3 py-2 rounded-3 fw-bold fs-7" style="background-color: #eff6ff; color: #1d4ed8;">Semua Level User</span>
            <a href="<?= $this->baseUrl('user'); ?>" class="text-secondary text-decoration-none fw-semibold fs-7">Reset Data</a>
        </div>

        <div class="table-responsive">
            <table class="table align-middle table-hover border-top border-light-subtle">
                <thead>
                    <tr class="text-secondary fs-7 fw-bold">
                        <th class="bg-white text-secondary py-3">No <i class="bi bi-chevron-expand fs-8"></i></th>
                        <th class="bg-white text-secondary py-3">ID User <i class="bi bi-chevron-expand fs-8"></i></th>
                        <th class="bg-white text-secondary py-3">Nama <i class="bi bi-chevron-expand fs-8"></i></th>
                        <th class="bg-white text-secondary py-3">Email <i class="bi bi-chevron-expand fs-8"></i></th>
                        <th class="bg-white text-secondary py-3">Level User <i class="bi bi-chevron-expand fs-8"></i></th>
                        <th class="bg-white text-secondary py-3">Role <i class="bi bi-chevron-expand fs-8"></i></th>
                        <th class="bg-white text-secondary py-3">Status <i class="bi bi-chevron-expand fs-8"></i></th>
                        <th class="bg-white text-secondary py-3">Action <i class="bi bi-chevron-expand fs-8"></i></th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (count($this->users) > 0): ?>
                        <?php $no = 1; foreach ($this->users as $user): ?>
                            <tr class="fs-7 text-dark">
                                <td class="text-secondary bg-white py-3"><?= $no++; ?></td>
                                <td class="fw-bold bg-white"><?= $user['id_user']; ?></td>
                                <td class="bg-white"><?= $user['fullName']; ?></td>
                                <td class="text-secondary bg-white"><?= $user['email']; ?></td>
                                <td class="bg-white"><?= $user['level_user']; ?></td>
                                <td class="bg-white"><?= $user['role']; ?></td>
                                <td class="bg-white">
                                    <?php 
                                        if ($user['status'] == 'Aktif') {
                                            $badgeStyle = 'background-color: #e8f5e9; color: #2e7d32;'; 
                                        } elseif ($user['status'] == 'Non-Aktif') {
                                            $badgeStyle = 'background-color: #ffebee; color: #c62828;'; 
                                        } else {
                                            $badgeStyle = 'background-color: #f5f5f5; color: #616161;'; 
                                        }
                                    ?>
                                    <span class="badge px-3 py-1.5 rounded-2 fw-bold" style="<?= $badgeStyle; ?>">
                                        <?= $user['status']; ?>
                                    </span>
                                </td>
                                <td class="bg-white">
                                    <div class="d-flex gap-2">
                                        <button class="btn btn-sm d-flex align-items-center gap-1 px-3 py-1.5 rounded-2 fw-semibold bg-white border border-warning text-warning">
                                            <i class="bi bi-pencil-fill fs-8"></i> Edit
                                        </button>
                                        <button class="btn btn-sm d-flex align-items-center gap-1 px-3 py-1.5 rounded-2 fw-semibold bg-white border border-primary text-primary">
                                            <i class="bi bi-eye-fill fs-8"></i> Detail
                                        </button>
                                    </div>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php else: ?>
                        <tr>
                            <td colspan="8" class="text-center py-5 text-secondary bg-white">Tidak ada data user.</td>
                        </tr>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>

        <div class="d-flex justify-content-between align-items-center mt-4 pt-3 border-top border-light-subtle">
            <span class="text-secondary fs-7">Menampilkan 1 sampai <?= count($this->users); ?> dari <?= count($this->users); ?> data</span>
            <nav>
                <ul class="pagination pagination-sm mb-0 gap-1">
                    <li class="page-item disabled"><a class="page-link rounded-2 border-light-subtle text-muted" href="#"><i class="bi bi-chevron-double-left"></i></a></li>
                    <li class="page-item disabled"><a class="page-link rounded-2 border-light-subtle text-muted" href="#"><i class="bi bi-chevron-left"></i></a></li>
                    <li class="page-item active"><a class="page-link rounded-2 px-3 fw-bold bg-primary border-primary" href="#">1</a></li>
                    <li class="page-item disabled"><a class="page-link rounded-2 border-light-subtle text-muted" href="#"><i class="bi bi-chevron-right"></i></a></li>
                    <li class="page-item disabled"><a class="page-link rounded-2 border-light-subtle text-muted" href="#"><i class="bi bi-chevron-double-right"></i></a></li>
                </ul>
            </nav>
        </div>

    </div>
</div>